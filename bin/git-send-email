#!/bin/bash

# git send-email --to="linux-nvdimm@lists.01.org" ./0001-xxx.patch --identity=intel

echo "$ git send-email --to=\"$1\" $2 --identity=intel"

git send-email --to="$1" $2 --identity=intel
