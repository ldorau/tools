#!/bin/bash -e

if [ "$1" == "" ]; then
	BRANCH=$(git log --merges -1 --pretty='%H')
else
	BRANCH=$1
fi

echo "$ git rebase -i $BRANCH"
if git rebase -i $BRANCH; then
	echo "Rebased on the commit:"
	git log -1 $BRANCH
fi
