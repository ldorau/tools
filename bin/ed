#!/bin/bash

if [ -f $1 ]; then
	vim $1
	exit 0
fi

FILE=$(which $1)
if [ "$FILE" == "" ]; then
	echo "Error: file not found: $1"
	exit 1
fi

if file $FILE | grep -q -e "ASCII text"; then
	vim $FILE
else
	echo "Error: not an ASCII file: $FILE"
	file $FILE
	exit 1
fi
