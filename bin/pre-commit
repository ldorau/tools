#!/bin/bash
# This is <GIT-REPO-DIR>/.git/hooks/pre-commit
# It has to be executable!
#

git diff --check --cached
if [ $? -ne 0 ]; then
	echo
	echo "Errors found! Fix them or commit changes with --no-verify option. Aborting commit..."
	echo
	exit 1
fi
exit 0

