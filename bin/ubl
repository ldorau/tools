#!/bin/bash

if [ "$1" = "" ]; then
	echo "Usage: ubl <ip>"
	exit 1
fi

NR=$(sudo ufw status numbered | grep -e "192.168.0.$1" | grep -e "DENY IN" | cut -d'[' -f2 | cut -d']' -f1 | awk '{print $1}')
[ "$NR" != "" ] && echo y | sudo ufw delete $NR
